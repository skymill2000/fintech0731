function plus(p1, p2) {
  console.log(p1 + p2);
}

const minus = (p1, p2) => {
  console.log(p1 - p2);
};

const mul = (p1, p2) => {
  console.log(p1 * p2);
};

const div = (p1, p2) => {
  console.log(p1 / p2);
};

plus(5, 10);
minus(5, 10);
mul(5, 10);
div(5, 10);

//사칙연산 나머지 arrow func 구현
